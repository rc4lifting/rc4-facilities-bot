import { Ballot, Slot } from "../database";
import Plotly from "plotly.js";

/**
 * A class that generates a visualisation of ballots or slots
 */
export class DVisualiser {
  /**
   * Given an allocation of ballots, generate a heatmap of 
   */
  static showBallots() {}
  static showSlots() {}
}